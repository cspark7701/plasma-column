diff --git a/Source/Particles/Collision/BackgroundMCC/BackgroundMCCCollision.H b/Source/Particles/Collision/BackgroundMCC/BackgroundMCCCollision.H
index 089262299..58c68fca9 100644
--- a/Source/Particles/Collision/BackgroundMCC/BackgroundMCCCollision.H
+++ b/Source/Particles/Collision/BackgroundMCC/BackgroundMCCCollision.H
@@ -32,7 +32,9 @@ public:
     BackgroundMCCCollision ( BackgroundMCCCollision&& )                  = delete;
     BackgroundMCCCollision& operator= ( BackgroundMCCCollision&& )       = delete;
 
-    [[nodiscard]] amrex::ParticleReal get_nu_max (amrex::Vector<ScatteringProcess> const& mcc_processes) const;
+    [[nodiscard]] amrex::ParticleReal get_nu_max (
+        amrex::Vector<ScatteringProcess> const& mcc_processes,
+        amrex::ParticleReal mass_for_energy) const;
 
     /** Perform the collisions
      *
@@ -68,6 +70,15 @@ public:
                                  amrex::Real t
                                  );
 
+    void doBackgroundIonImpactIonization (
+                                 int lev,
+                                 amrex::LayoutData<amrex::Real>* cost,
+                                 WarpXParticleContainer& projectile_species,
+                                 WarpXParticleContainer& electron_species,
+                                 WarpXParticleContainer& ion_species,
+                                 amrex::Real t
+                                 );
+
 private:
 
     amrex::Vector<ScatteringProcess> m_scattering_processes;
@@ -75,6 +86,15 @@ private:
     amrex::Gpu::DeviceVector<ScatteringProcess::Executor> m_scattering_processes_exe;
     amrex::Gpu::DeviceVector<ScatteringProcess::Executor> m_ionization_processes_exe;
 
+    amrex::Vector<ScatteringProcess> m_ion_impact_ionization_processes;
+    amrex::Gpu::DeviceVector<ScatteringProcess::Executor> m_ion_impact_ionization_processes_exe;
+    bool ion_impact_ionization_flag = false;
+    amrex::ParticleReal m_total_collision_prob_ion_impact_ioniz = 0;
+    amrex::ParticleReal m_nu_max_ion_impact_ioniz = 0;
+    std::string m_ion_impact_electron_species_name;
+    std::string m_ion_impact_ion_species_name;
+    amrex::ParticleReal m_ion_impact_secondary_electron_energy = 1.0;
+
     bool init_flag = false;
     bool ionization_flag = false;
 
diff --git a/Source/Particles/Collision/BackgroundMCC/BackgroundMCCCollision.cpp b/Source/Particles/Collision/BackgroundMCC/BackgroundMCCCollision.cpp
index 779276aca..dab95c598 100644
--- a/Source/Particles/Collision/BackgroundMCC/BackgroundMCCCollision.cpp
+++ b/Source/Particles/Collision/BackgroundMCC/BackgroundMCCCollision.cpp
@@ -7,6 +7,7 @@
 #include "BackgroundMCCCollision.H"
 
 #include "ImpactIonization.H"
+#include "IonImpactIonization.H"
 #include "Particles/Algorithms/KineticEnergy.H"
 #include "Particles/ParticleCreation/FilterCopyTransform.H"
 #include "Particles/ParticleCreation/SmartCopy.H"
@@ -136,6 +137,31 @@ BackgroundMCCCollision::BackgroundMCCCollision (std::string const& collision_nam
             m_species_names.push_back(secondary_species);
 
             m_ionization_processes.push_back(std::move(process));
+        } else if (process.type() == ScatteringProcessType::ION_IMPACT_IONIZATION) {
+            WARPX_ALWAYS_ASSERT_WITH_MESSAGE(
+                !ion_impact_ionization_flag,
+                "Background MCC only supports a single ion_impact_ionization process");
+
+            WARPX_ALWAYS_ASSERT_WITH_MESSAGE(
+                !ionization_flag,
+                "Do not combine electron-impact ionization and ion-impact ionization in the same background_mcc collision.");
+
+            ion_impact_ionization_flag = true;
+
+            pp_collision_name.get(
+                "ion_impact_ionization_electron_species",
+                m_ion_impact_electron_species_name);
+
+            pp_collision_name.get(
+                "ion_impact_ionization_ion_species",
+                m_ion_impact_ion_species_name);
+
+            utils::parser::queryWithParser(
+                pp_collision_name,
+                "ion_impact_ionization_electron_energy",
+                m_ion_impact_secondary_electron_energy);
+
+            m_ion_impact_ionization_processes.push_back(std::move(process));
         } else {
             m_scattering_processes.push_back(std::move(process));
         }
@@ -144,18 +170,26 @@ BackgroundMCCCollision::BackgroundMCCCollision (std::string const& collision_nam
 #ifdef AMREX_USE_GPU
     amrex::Gpu::HostVector<ScatteringProcess::Executor> h_scattering_processes_exe;
     amrex::Gpu::HostVector<ScatteringProcess::Executor> h_ionization_processes_exe;
+    amrex::Gpu::HostVector<ScatteringProcess::Executor> h_ion_impact_ionization_processes_exe;
     for (auto const& p : m_scattering_processes) {
         h_scattering_processes_exe.push_back(p.executor());
     }
     for (auto const& p : m_ionization_processes) {
         h_ionization_processes_exe.push_back(p.executor());
     }
+    for (auto const& p : m_ion_impact_ionization_processes) {
+        h_ion_impact_ionization_processes_exe.push_back(p.executor());
+    }
     m_scattering_processes_exe.resize(h_scattering_processes_exe.size());
     m_ionization_processes_exe.resize(h_ionization_processes_exe.size());
+    m_ion_impact_ionization_processes_exe.resize(h_ion_impact_ionization_processes_exe.size());
     amrex::Gpu::copyAsync(amrex::Gpu::hostToDevice, h_scattering_processes_exe.begin(),
                           h_scattering_processes_exe.end(), m_scattering_processes_exe.begin());
     amrex::Gpu::copyAsync(amrex::Gpu::hostToDevice, h_ionization_processes_exe.begin(),
                           h_ionization_processes_exe.end(), m_ionization_processes_exe.begin());
+    amrex::Gpu::copyAsync(amrex::Gpu::hostToDevice, h_ion_impact_ionization_processes_exe.begin(),
+                          h_ion_impact_ionization_processes_exe.end(),
+                          m_ion_impact_ionization_processes_exe.begin());
     amrex::Gpu::streamSynchronize();
 #else
     for (auto const& p : m_scattering_processes) {
@@ -164,6 +198,9 @@ BackgroundMCCCollision::BackgroundMCCCollision (std::string const& collision_nam
     for (auto const& p : m_ionization_processes) {
         m_ionization_processes_exe.push_back(p.executor());
     }
+    for (auto const& p : m_ion_impact_ionization_processes) {
+        m_ion_impact_ionization_processes_exe.push_back(p.executor());
+    }
 #endif
 }
 
@@ -171,7 +208,9 @@ BackgroundMCCCollision::BackgroundMCCCollision (std::string const& collision_nam
  *  ranges from 1e-4 to 5000 eV in 0.2 eV increments
  */
 amrex::ParticleReal
-BackgroundMCCCollision::get_nu_max(amrex::Vector<ScatteringProcess> const& mcc_processes) const
+BackgroundMCCCollision::get_nu_max (
+    amrex::Vector<ScatteringProcess> const& mcc_processes,
+    amrex::ParticleReal const mass_for_energy) const
 {
     using namespace amrex::literals;
     amrex::ParticleReal nu, nu_max = 0.0;
@@ -203,7 +242,7 @@ BackgroundMCCCollision::get_nu_max(amrex::Vector<ScatteringProcess> const& mcc_p
         // calculate collision frequency
         nu = (
               m_max_background_density
-              * std::sqrt(2.0_prt / m_mass1 * PhysConst::q_e)
+              * std::sqrt(2.0_prt / mass_for_energy * PhysConst::q_e)
               * sigma_E * std::sqrt(E)
               );
         if (nu > nu_max) {
@@ -222,6 +261,14 @@ BackgroundMCCCollision::doCollisions (amrex::Real cur_time, amrex::Real dt, Mult
     using namespace amrex::literals;
 
     auto& species1 = mypc->GetParticleContainerFromName(m_species_names[0]);
+
+    WarpXParticleContainer* ionimpact_electrons = nullptr;
+    WarpXParticleContainer* ionimpact_ions = nullptr;
+    if (ion_impact_ionization_flag) {
+        ionimpact_electrons = &mypc->GetParticleContainerFromName(m_ion_impact_electron_species_name);
+        ionimpact_ions = &mypc->GetParticleContainerFromName(m_ion_impact_ion_species_name);
+    }
+
     // this is a very ugly hack to have species2 be a reference and be
     // defined in the scope of doCollisions
     auto& species2 = (
@@ -233,8 +280,23 @@ BackgroundMCCCollision::doCollisions (amrex::Real cur_time, amrex::Real dt, Mult
     if (!init_flag) {
         m_mass1 = species1.getMass();
 
+        if (ion_impact_ionization_flag) {
+            if (m_background_mass == -1) {
+                m_background_mass = ionimpact_ions->getMass();
+            }
+            const auto mu = m_mass1*m_background_mass/(m_mass1 + m_background_mass);
+            m_nu_max_ion_impact_ioniz = get_nu_max(m_ion_impact_ionization_processes, mu);
+            const auto coll_n = m_nu_max_ion_impact_ioniz * dt;
+            m_total_collision_prob_ion_impact_ioniz = 1.0_prt - std::exp(-coll_n);
+            if (coll_n > 0.1_prt) {
+                ablastr::warn_manager::WMRecordWarning(
+                    "BackgroundMCC Collisions",
+                    "dt is too large for accurate ion-impact ionization MCC.");
+            }
+        }
+
         // calculate maximum collision frequency without ionization
-        m_nu_max = get_nu_max(m_scattering_processes);
+        m_nu_max = get_nu_max(m_scattering_processes, m_mass1);
 
         // calculate total collision probability
         auto coll_n = m_nu_max * dt;
@@ -252,7 +314,7 @@ BackgroundMCCCollision::doCollisions (amrex::Real cur_time, amrex::Real dt, Mult
 
         if (ionization_flag) {
             // calculate maximum collision frequency for ionization
-            m_nu_max_ioniz = get_nu_max(m_ionization_processes);
+            m_nu_max_ioniz = get_nu_max(m_ionization_processes, m_mass1);
 
             // calculate total ionization probability
             auto coll_n_ioniz = m_nu_max_ioniz * dt;
@@ -319,6 +381,11 @@ BackgroundMCCCollision::doCollisions (amrex::Real cur_time, amrex::Real dt, Mult
         if (ionization_flag) {
             doBackgroundIonization(lev, cost, species1, species2, cur_time);
         }
+
+        if (ion_impact_ionization_flag) {
+            doBackgroundIonImpactIonization(
+                lev, cost, species1, *ionimpact_electrons, *ionimpact_ions, cur_time);
+        }
     }
 }
 
@@ -531,3 +598,82 @@ void BackgroundMCCCollision::doBackgroundIonization
         }
     }
 }
+
+
+void BackgroundMCCCollision::doBackgroundIonImpactIonization (
+    int lev,
+    amrex::LayoutData<amrex::Real>* cost,
+    WarpXParticleContainer& projectile_species,
+    WarpXParticleContainer& electron_species,
+    WarpXParticleContainer& ion_species,
+    amrex::Real t)
+{
+    ABLASTR_PROFILE("BackgroundMCCCollision::doBackgroundIonImpactIonization()");
+
+    const SmartCopyFactory copy_factory_electron(projectile_species, electron_species);
+    const SmartCopyFactory copy_factory_ion(projectile_species, ion_species);
+
+    const auto CopyElectron = copy_factory_electron.getSmartCopy();
+    const auto CopyIon = copy_factory_ion.getSmartCopy();
+
+    const auto Filter = IonImpactIonizationFilterFunc(
+        m_ion_impact_ionization_processes[0],
+        m_mass1,
+        m_background_mass,
+        m_total_collision_prob_ion_impact_ioniz,
+        m_nu_max_ion_impact_ioniz,
+        m_background_density_func,
+        t);
+
+    const amrex::ParticleReal sqrt_kb_m_gas =
+        std::sqrt(PhysConst::kb / m_background_mass);
+
+#ifdef AMREX_USE_OMP
+#pragma omp parallel if (amrex::Gpu::notInLaunchRegion())
+#endif
+    for (WarpXParIter pti(projectile_species, lev); pti.isValid(); ++pti)
+    {
+        if (cost && WarpX::load_balance_costs_update_algo == LoadBalanceCostsUpdateAlgo::Timers) {
+            amrex::Gpu::synchronize();
+        }
+
+        auto wt = static_cast<amrex::Real>(amrex::second());
+
+        auto& projectile_tile = projectile_species.ParticlesAt(lev, pti);
+        auto& electron_tile = electron_species.ParticlesAt(lev, pti);
+        auto& ion_tile = ion_species.ParticlesAt(lev, pti);
+
+        const auto np_electron = electron_tile.numParticles();
+        const auto np_ion = ion_tile.numParticles();
+
+        auto Transform = IonImpactIonizationTransformFunc(
+            m_ion_impact_ionization_processes[0].getEnergyPenalty(),
+            m_mass1,
+            m_ion_impact_secondary_electron_energy,
+            sqrt_kb_m_gas,
+            m_background_temperature_func,
+            t);
+
+        const auto num_added = filterCopyTransformParticles<1>(
+            electron_species,
+            ion_species,
+            electron_tile,
+            ion_tile,
+            projectile_tile,
+            np_electron,
+            np_ion,
+            Filter,
+            CopyElectron,
+            CopyIon,
+            Transform);
+
+        setNewParticleIDs(electron_tile, np_electron, num_added);
+        setNewParticleIDs(ion_tile, np_ion, num_added);
+
+        if (cost && WarpX::load_balance_costs_update_algo == LoadBalanceCostsUpdateAlgo::Timers) {
+            amrex::Gpu::synchronize();
+            wt = static_cast<amrex::Real>(amrex::second()) - wt;
+            amrex::HostDevice::Atomic::Add(&(*cost)[pti.index()], wt);
+        }
+    }
+}
diff --git a/Source/Particles/Collision/BackgroundMCC/IonImpactIonization.H b/Source/Particles/Collision/BackgroundMCC/IonImpactIonization.H
new file mode 100644
index 000000000..1412a7a11
--- /dev/null
+++ b/Source/Particles/Collision/BackgroundMCC/IonImpactIonization.H
@@ -0,0 +1,232 @@
+/* Copyright 2026
+ *
+ * This file is an experimental extension for WarpX BackgroundMCC.
+ *
+ * Ion-impact ionization process:
+ *
+ *     ion_projectile + neutral_gas -> ion_projectile + gas_ion + electron
+ *
+ * Example:
+ *
+ *     p + H2 -> p + H2+ + e-
+ *     p + Kr -> p + Kr+ + e-
+ *
+ * The parent projectile remains in the source species.  One electron and one
+ * gas-ion macroparticle are created at the projectile position whenever the
+ * MCC event is accepted.
+ */
+
+#ifndef WARPX_PARTICLES_COLLISION_ION_IMPACT_IONIZATION_H_
+#define WARPX_PARTICLES_COLLISION_ION_IMPACT_IONIZATION_H_
+
+#include "Particles/Collision/ScatteringProcess.H"
+#include "Particles/Algorithms/KineticEnergy.H"
+#include "Utils/ParticleUtils.H"
+#include "Utils/WarpXConst.H"
+
+#include <AMReX_Random.H>
+#include <AMReX_REAL.H>
+
+/**
+ * \brief Filter functor for ion-impact ionization.
+ *
+ * The cross-section table is assumed to be tabulated against center-of-mass
+ * collision energy in eV.  The background neutral gas is treated as stationary
+ * in this first implementation; at 30 keV proton energy, thermal neutral
+ * velocities at 300 K are negligible for H2/Kr.
+ */
+class IonImpactIonizationFilterFunc
+{
+public:
+    IonImpactIonizationFilterFunc (
+        ScatteringProcess const& mcc_process,
+        double const projectile_mass,
+        double const background_mass,
+        amrex::ParticleReal const total_collision_prob,
+        amrex::ParticleReal const nu_max,
+        amrex::ParserExecutor<4> const& n_a_func,
+        amrex::Real const t )
+        : m_mcc_process(mcc_process.executor()),
+          m_projectile_mass(projectile_mass),
+          m_background_mass(background_mass),
+          m_total_collision_prob(total_collision_prob),
+          m_nu_max(nu_max),
+          m_n_a_func(n_a_func),
+          m_t(t)
+    {}
+
+    template <typename PData>
+    AMREX_GPU_HOST_DEVICE AMREX_FORCE_INLINE
+    bool operator() (
+        PData& ptd,
+        int const i,
+        amrex::RandomEngine const& engine) const noexcept
+    {
+        using namespace amrex;
+
+        if (Random(engine) > m_total_collision_prob) {
+            return false;
+        }
+
+        const auto& p = ptd.getSuperParticle(i);
+
+        ParticleReal x, y, z;
+        get_particle_position(p, x, y, z);
+
+        const ParticleReal n_a = m_n_a_func(x, y, z, m_t);
+
+        const ParticleReal ux = ptd.m_rdata[PIdx::ux][i];
+        const ParticleReal uy = ptd.m_rdata[PIdx::uy][i];
+        const ParticleReal uz = ptd.m_rdata[PIdx::uz][i];
+
+        // PIdx::u is gamma*v.  For the present low-energy cyclotron-injection
+        // application gamma ~= 1, but use WarpX's collision-energy helper.
+        const ParticleReal u2 = ux*ux + uy*uy + uz*uz;
+        const ParticleReal u_coll = std::sqrt(u2);
+
+        double gamma, E_coll;
+        ParticleUtils::getCollisionEnergy(
+            u2, m_projectile_mass, m_background_mass, gamma, E_coll);
+
+        const ParticleReal sigma_E =
+            m_mcc_process.getCrossSection(static_cast<ParticleReal>(E_coll));
+
+        const ParticleReal nu_i = n_a * sigma_E * u_coll / m_nu_max;
+
+        return (Random(engine) <= nu_i);
+    }
+
+private:
+    ScatteringProcess::Executor m_mcc_process;
+    double m_projectile_mass;
+    double m_background_mass;
+    amrex::ParticleReal m_total_collision_prob = 0;
+    amrex::ParticleReal m_nu_max = 0;
+    amrex::ParserExecutor<4> m_n_a_func;
+    amrex::Real m_t;
+};
+
+
+/**
+ * \brief Transform functor for ion-impact ionization.
+ *
+ * For each accepted event, this transform:
+ *   1. subtracts the ionization energy from the parent ion projectile;
+ *   2. creates a secondary electron with an isotropic fixed kinetic energy;
+ *   3. creates a gas ion with a thermal velocity sampled from the neutral gas
+ *      temperature.
+ *
+ * The product macroparticle weights and positions are inherited through
+ * SmartCopy before this transform is called.
+ */
+class IonImpactIonizationTransformFunc
+{
+public:
+    IonImpactIonizationTransformFunc (
+        amrex::ParticleReal const energy_cost_eV,
+        double const projectile_mass,
+        amrex::ParticleReal const electron_energy_eV,
+        amrex::ParticleReal const sqrt_kb_m_gas,
+        amrex::ParserExecutor<4> const& T_a_func,
+        amrex::Real const t )
+        : m_energy_cost_eV(energy_cost_eV),
+          m_projectile_mass(projectile_mass),
+          m_electron_energy_eV(electron_energy_eV),
+          m_sqrt_kb_m_gas(sqrt_kb_m_gas),
+          m_T_a_func(T_a_func),
+          m_t(t)
+    {}
+
+    template <typename ElectronData, typename IonData, typename SrcData>
+    AMREX_GPU_HOST_DEVICE AMREX_FORCE_INLINE
+    void operator() (
+        ElectronData& dst_e,
+        IonData& dst_i,
+        SrcData& src,
+        int const i_src,
+        int const i_dst_e,
+        int const i_dst_i,
+        amrex::RandomEngine const& engine) const noexcept
+    {
+        using namespace amrex;
+        using namespace amrex::literals;
+
+        const auto& p = src.getSuperParticle(i_src);
+
+        ParticleReal x, y, z;
+        get_particle_position(p, x, y, z);
+
+        // ------------------------------------------------------------------
+        // Parent projectile: subtract ionization energy, preserving direction.
+        // ------------------------------------------------------------------
+        auto& ux = src.m_rdata[PIdx::ux][i_src];
+        auto& uy = src.m_rdata[PIdx::uy][i_src];
+        auto& uz = src.m_rdata[PIdx::uz][i_src];
+
+        constexpr auto eV = PhysConst::q_e;
+        constexpr auto c2 = PhysConst::c2;
+
+        const ParticleReal u2 = ux*ux + uy*uy + uz*uz;
+        const auto E_old_J =
+            Algorithms::KineticEnergy(ux, uy, uz,
+                                      static_cast<ParticleReal>(m_projectile_mass));
+
+        const auto E_loss_J = m_energy_cost_eV * eV;
+
+        if ((u2 > 0._prt) && (E_old_J > E_loss_J)) {
+            const auto E_new_J = E_old_J - E_loss_J;
+            const auto mc2 = m_projectile_mass * c2;
+            const auto u_new =
+                std::sqrt(E_new_J * (E_new_J + 2._prt*mc2)
+                          * PhysConst::inv_c2)
+                / m_projectile_mass;
+            const auto scale = static_cast<ParticleReal>(u_new / std::sqrt(u2));
+            ux *= scale;
+            uy *= scale;
+            uz *= scale;
+        }
+
+        // ------------------------------------------------------------------
+        // Product electron: isotropic emission with fixed secondary energy.
+        // A later refinement can sample the Rudd differential spectrum.
+        // ------------------------------------------------------------------
+        auto& e_ux = dst_e.m_rdata[PIdx::ux][i_dst_e];
+        auto& e_uy = dst_e.m_rdata[PIdx::uy][i_dst_e];
+        auto& e_uz = dst_e.m_rdata[PIdx::uz][i_dst_e];
+
+        const auto Ee_J = m_electron_energy_eV * eV;
+        const auto me_c2 = PhysConst::m_e * c2;
+        const ParticleReal ue =
+            (Ee_J > 0._prt)
+            ? static_cast<ParticleReal>(
+                std::sqrt(Ee_J * (Ee_J + 2._prt*me_c2) * PhysConst::inv_c2)
+                / PhysConst::m_e )
+            : 0._prt;
+
+        ParticleUtils::RandomizeVelocity(e_ux, e_uy, e_uz, ue, engine);
+
+        // ------------------------------------------------------------------
+        // Product gas ion: sampled from neutral background temperature.
+        // ------------------------------------------------------------------
+        auto& i_ux = dst_i.m_rdata[PIdx::ux][i_dst_i];
+        auto& i_uy = dst_i.m_rdata[PIdx::uy][i_dst_i];
+        auto& i_uz = dst_i.m_rdata[PIdx::uz][i_dst_i];
+
+        const ParticleReal ion_vel_std =
+            m_sqrt_kb_m_gas * std::sqrt(m_T_a_func(x, y, z, m_t));
+
+        i_ux = ion_vel_std * RandomNormal(0._prt, 1._prt, engine);
+        i_uy = ion_vel_std * RandomNormal(0._prt, 1._prt, engine);
+        i_uz = ion_vel_std * RandomNormal(0._prt, 1._prt, engine);
+    }
+
+private:
+    amrex::ParticleReal m_energy_cost_eV = 0;
+    double m_projectile_mass = 0;
+    amrex::ParticleReal m_electron_energy_eV = 1;
+    amrex::ParticleReal m_sqrt_kb_m_gas = 0;
+    amrex::ParserExecutor<4> m_T_a_func;
+    amrex::Real m_t;
+};
+
+#endif // WARPX_PARTICLES_COLLISION_ION_IMPACT_IONIZATION_H_
diff --git a/Source/Particles/Collision/ScatteringProcess.H b/Source/Particles/Collision/ScatteringProcess.H
index 524a610ea..62086b6ec 100644
--- a/Source/Particles/Collision/ScatteringProcess.H
+++ b/Source/Particles/Collision/ScatteringProcess.H
@@ -25,6 +25,7 @@ enum class ScatteringProcessType {
     CHARGE_EXCHANGE,
     EXCITATION,
     IONIZATION,
+    ION_IMPACT_IONIZATION,
     FORWARD,
 };
 
diff --git a/Source/Particles/Collision/ScatteringProcess.cpp b/Source/Particles/Collision/ScatteringProcess.cpp
index 57d37065e..ba0e1dc2c 100644
--- a/Source/Particles/Collision/ScatteringProcess.cpp
+++ b/Source/Particles/Collision/ScatteringProcess.cpp
@@ -10,6 +10,9 @@
 
 #include "Utils/TextMsg.H"
 
+#include <fstream>
+#include <sstream>
+
 ScatteringProcess::ScatteringProcess (
                         const std::string& scattering_process,
                         const std::string& cross_section_file,
@@ -86,6 +89,8 @@ ScatteringProcess::parseProcessType(const std::string& scattering_process)
         return ScatteringProcessType::TWOPRODUCT_REACTION;
     } else if (scattering_process == "ionization") {
         return ScatteringProcessType::IONIZATION;
+    } else if (scattering_process == "ion_impact_ionization") {
+        return ScatteringProcessType::ION_IMPACT_IONIZATION;
     } else if (scattering_process.find("excitation") != std::string::npos) {
         return ScatteringProcessType::EXCITATION;
     } else if (scattering_process.find("forward") != std::string::npos) {
@@ -102,14 +107,29 @@ ScatteringProcess::readCrossSectionFile (
                                   amrex::Gpu::HostVector<amrex::ParticleReal>& sigmas )
 {
     std::ifstream infile(cross_section_file);
-    if(!infile.is_open()) { WARPX_ABORT_WITH_MESSAGE("Failed to open cross-section data file"); }
+    if(!infile.is_open()) { WARPX_ABORT_WITH_MESSAGE("Failed to open cross-section data file: " + cross_section_file); }
+
+    std::string line;
+    while (std::getline(infile, line)) {
+        // Allow comments and blank lines in cross-section files.
+        const auto first = line.find_first_not_of(" \t");
+        if (first == std::string::npos || line[first] == '#') {
+            continue;
+        }
 
-    amrex::ParticleReal energy, sigma;
-    while (infile >> energy >> sigma) {
-        energies.push_back(energy);
-        sigmas.push_back(sigma);
+        std::istringstream iss(line);
+        amrex::ParticleReal energy, sigma;
+        if (iss >> energy >> sigma) {
+            energies.push_back(energy);
+            sigmas.push_back(sigma);
+        }
     }
-    if (infile.bad()) { WARPX_ABORT_WITH_MESSAGE("Failed to read cross-section data from file."); }
+
+    WARPX_ALWAYS_ASSERT_WITH_MESSAGE(
+        (!energies.empty()),
+        "No cross-section data were read from file: " + cross_section_file);
+
+    if (infile.bad()) { WARPX_ABORT_WITH_MESSAGE("Failed to read cross-section data from file: " + cross_section_file); }
     infile.close();
 }
 
